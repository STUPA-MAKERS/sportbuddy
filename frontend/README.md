# Sportbuddy Frontend

Angular 21 + PrimeNG. Konfiguration und Deployment: siehe root `README.md`.

## Entwicklung

```bash
npm install
npm start
```

API-Calls gehen gegen `/api` (relativ — Proxy konfigurieren).
